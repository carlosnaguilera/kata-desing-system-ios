import Foundation

enum Spacing {
    // Although in Figma we can read about "the 8-pt grid", the design of the alert uses
    // a vertical spacing of 12
    static let medium: CGFloat = 12
    static let large: CGFloat = 16
    static let extraLarge: CGFloat = 24
}
